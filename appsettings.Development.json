{
    "Logging": {
        "LogLevel": {
            "Default": "Information",
            "Microsoft.AspNetCore": "Warning"
        }
    },
    "AzureOpenAI": {
        /*
        You can set these secrets here in config, but it's better to use the dotnet-secrets tool:

        cd <directory_containing_this_project>
        dotnet user-secrets set "AzureOpenAI:Endpoint" "https://SOMETHING.openai.azure.com/"
        dotnet user-secrets set "AzureOpenAI:Key" YourKeyHere
        */
        "Endpoint": "",
        "Key": ""
    },
    "CustomOpenAI": {
        /*
        You can set these secrets here in config, but it's better to use the dotnet-secrets tool:

        cd <directory_containing_this_project>
        dotnet user-secrets set "AzureOpenAI:Endpoint" "https://SOMETHING.openai.azure.com/"
        dotnet user-secrets set "AzureOpenAI:Key" YourKeyHere
        */
        "Endpoint": "https://api.cerebras.ai/v1",
        "Key": "Your-Key-Here",
        "Model": "llama-3.3-70b"
    }
}
